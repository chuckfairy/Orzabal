/**
 * Pi commands for system
 */
#pragma once

namespace Orza { namespace Pi { namespace Config {

const char * SHUTDOWN_COMMAND = "sudo shutdown 0";

const char * RESTART_COMMAND = "sudo reboot";

}; }; };
